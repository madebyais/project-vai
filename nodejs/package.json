{
  "name": "node-vai",
  "version": "1.0.0",
  "description": "Backend developer test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist/ && babel ./src --out-dir dist/ --ignore ./node_modules,./babelrc,.package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node index.js --harmony"
  },
  "author": "Faris",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "async": "^2.5.0",
    "body-parser": "^1.18.2",
    "compression": "^1.7.1",
    "cors": "^2.8.4",
    "ejs": "^2.5.7",
    "express": "^4.16.2",
    "helmet": "^3.9.0",
    "multer": "^1.3.0",
    "mysql": "^2.15.0",
    "pipa": "0.0.8",
    "regression": "^2.0.0",
    "sequelize": "^4.16.0"
  }
}
